[project]
name = "reinfolib-local-viewer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.135.2",
    "playwright>=1.58.0",
    "uvicorn>=0.42.0",
]
